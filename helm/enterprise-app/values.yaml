replicaCount: 2

image:
  repository: avadhootpatil/enterprise-app
  tag: latest

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  host: enterprise.local

hpa:
  minReplicas: 2
  maxReplicas: 10
  cpu: 70
